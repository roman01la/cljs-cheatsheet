===== Name
clojure.string/join

===== Signature
[coll]
[separator coll]

===== Description

Возвращает строку, состоящей из всех элементов в последовательности `coll`,
возвращаемой `(seq coll)`. Если задан опциональный разделитель - он вставляется
между элементами.

===== Related
