===== Name
cljs.core/if-let

===== Type
macro

===== Signature
[[x test] then]
[[x test] then else]

===== Description

Если `test` будет логическим true, результат выполнения `then` будет присвоен в
`x`. В противном случае, возвращается результат выполнения `else` без присваивания.

Поумолчанию `else` приравнивается к nil.

===== Related
cljs.core/when-let
cljs.core/if
