===== Name
cljs.core/letfn

===== Type
специальная форма

===== Signature
[fnspecs & body]

===== Description

Принимает вектор определений функций `fnspecs` и привязывает их к их именам.
Все функции доступны в `body` и во всех других функциях в определении.

`fnspecs` должен быть вектором с четным количеством форм. Смотри `let`.

`letfn` является [специальной формой] в ClojureScript.

[специальной формой]:http://clojure.org/special_forms

===== Related
cljs.core/let
