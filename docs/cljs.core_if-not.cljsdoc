===== Name
cljs.core/if-not

===== Type
macro

===== Signature
[test then]
[test then else]

===== Description

Если `test` будет false или nil, возвращается результат выполнения `then`. В противном случае, evaluates
возвращается результат выполнения `else`. Поумолчанию `else` приравнивается к nil, если его нету.

===== Related
cljs.core/if
cljs.core/when-not
